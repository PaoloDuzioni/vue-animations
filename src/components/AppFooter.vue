<template>
    <footer class="text-center">Animating Vue</footer>
</template>

<style scoped>
footer {
    padding: 1.5rem;
    font-weight: var(--weight-bold);
    color: var(--bg-col);
    background: linear-gradient(
        to right,
        var(--primary),
        var(--secondary),
        var(--tertiary)
    );
}
</style>
